import * as R from 'ramda';
import request from 'superagent';

import phones from 'api/mock-phones';
import categories from 'api/mock-categories';

export const fetchPhones = async () => {
    // for Test
    // const {body} = await request.get('http://www.mocky.io/v2/5918bc6b120000701040dbec');
    // return body.phones;
    return new Promise((resolve, reject) => {
        resolve(phones);
        //reject('error');
    })
};

export const loadMorePhones = async ({offset}) => {
    // fetch.get(`http://google.com/api/phones?offset=${offset}`);
    return new Promise((resolve, reject) => {
        resolve(phones);
        //reject('error');
    })
};

export const fetchPhoneById = async (id) => {
    return new Promise((resolve, reject) =>{
        const phone = R.find(R.propEq('id', id), phones); // found id from phones , when id = id;
        resolve(phone);
    })
};

export const fetchCategories = async () => {
    return new Promise((resolve, reject) => {
        resolve(categories);
        //reject('error');
    })
};